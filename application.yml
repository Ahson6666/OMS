"Add file" → Name: `src/main/resources/application.yml`

server:
  port: 8080
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oms
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

Commit: "config: add application properties"

